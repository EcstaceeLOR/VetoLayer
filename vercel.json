{
  "buildCommand": "pnpm --filter @vetolayer/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/internal/retention", "schedule": "0 3 * * *" }
  ]
}
